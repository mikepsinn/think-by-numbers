# Full `npm run build` used to run here. That deletes and rewrites
# ~2,300 `_site` files plus a Pagefind index on every commit, which
# pegs Defender and Windows Search for minutes. Vercel already builds
# on push.
